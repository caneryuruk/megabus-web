# Gerçek kimlik bilgileri — repoya GİTMEZ (WiFi şifresi + Firebase URL içerir)
firmware/esp8266_firebase_bridge/secrets.h

# Ortam ve yerel dosyalar
.env
.env.*
*.local
.vercel
node_modules
.DS_Store
Thumbs.db
